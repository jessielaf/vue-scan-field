/*#if _FRAMEWORK == 'vuetify'
import frameworkMapping from '@/input-components/vuetify'
//#else */
import frameworkMapping from '@/input-components/quasar'
//#endif

export default frameworkMapping
